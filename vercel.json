{
  "version": 2,
  "buildCommand": "cd thryve-frontend && npm install && npm run build",
  "outputDirectory": "thryve-frontend/dist",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/thryve-frontend/dist/$1"
    }
  ]
}